---
// Modal de confirmación de cita agendada
---

<!-- Success Modal -->
<div id="success-modal" class="fixed inset-0 bg-black/80 backdrop-blur-sm z-[10000] hidden flex items-center justify-center p-4" style="display: none;">
  <div class="bg-gradient-to-br from-[#1a1a1a] to-[#232323] rounded-2xl w-full max-w-md shadow-2xl border-2 border-[#82e256]/30 animate-scale-in">

    <!-- Header -->
    <div class="bg-gradient-to-r from-[#82e256] to-[#6bc73d] p-8 rounded-t-2xl text-center">
   
      <h2 class="text-3xl font-bold text-white">¡Asesoría Agendada!</h2>
    </div>

    <!-- Content -->
    <div class="p-8 space-y-6">
      <div class="text-center space-y-4">
        <p class="text-white/70 text-base">
         Pronto nos contactaremos con usted para confirmar los detalles de su sesión.
        </p>
      </div>

      <!-- Details -->
      <div class="bg-black/30 rounded-xl p-4 space-y-3">
        <div class="flex items-start gap-3">
          <svg class="w-5 h-5 text-[#82e256] mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
          </svg>
          <div>
            <p class="text-white/90 text-sm">
              Recibirás un email de confirmación en:
            </p>
            <p id="modal-email" class="text-[#82e256] font-semibold"></p>
          </div>
        </div>

        <div class="flex items-start gap-3">
          <svg class="w-5 h-5 text-[#82e256] mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
          </svg>
          <div>
            <p class="text-white/90 text-sm">Fecha y hora:</p>
            <p id="modal-datetime" class="text-white font-semibold"></p>
          </div>
        </div>

        <div class="flex items-start gap-3">
          <svg class="w-5 h-5 text-[#82e256] mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
          </svg>
          <div>
            <p class="text-white/90 text-sm">Servicio:</p>
            <p id="modal-service" class="text-white font-semibold"></p>
          </div>
        </div>
      </div>

      <!-- Next Steps -->
      <div class="bg-gradient-to-r from-[#82e256]/10 to-[#6bc73d]/10 border border-[#82e256]/20 rounded-xl p-4">
        <h3 class="text-white font-semibold mb-2 flex items-center gap-2">
          <svg class="w-5 h-5 text-[#82e256]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          Próximos pasos
        </h3>

          <p class="flex items-start gap-2">
            <span class="text-[#82e256] mt-1">•</span>
            <span>Revisa tu email para la confirmación, luego Prepara las preguntas que quieras hacer, Te contactaremos 24h antes para confirmar o si existe algún cambio</span>
          </p>
      </div>

      <!-- Close Button -->
      <button
        id="close-success-modal"
        class="w-full px-6 py-4 bg-gradient-to-r from-[#82e256] to-[#6bc73d] hover:from-[#6bc73d] hover:to-[#5ba62e] text-black font-bold rounded-xl transition-all duration-300 transform hover:scale-[1.02] shadow-lg hover:shadow-xl flex items-center justify-center gap-2"
      >
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
        </svg>
        <span>Entendido</span>
      </button>
    </div>
  </div>
</div>

<style>
  @keyframes scale-in {
    from {
      opacity: 0;
      transform: scale(0.9);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  .animate-scale-in {
    animation: scale-in 0.3s ease-out;
  }

  #success-modal {
    display: none;
  }

  #success-modal.show {
    display: flex !important;
  }
</style>

<script is:inline>
  // Función global para abrir el modal de éxito
  window.showSuccessModal = function(appointmentData) {
    const modal = document.getElementById('success-modal');

    // Llenar datos
    document.getElementById('modal-email').textContent = appointmentData.email || '';
    document.getElementById('modal-datetime').textContent =
      `${appointmentData.date ? new Date(appointmentData.date + 'T00:00:00').toLocaleDateString('es-ES', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) : ''} a las ${appointmentData.time || ''}`;
    document.getElementById('modal-service').textContent = appointmentData.service || 'Asesoría General';

    // Mostrar modal
    modal.classList.add('show');
  };

  // Cerrar modal
  document.addEventListener('DOMContentLoaded', function() {
    const closeBtn = document.getElementById('close-success-modal');
    const modal = document.getElementById('success-modal');

    if (closeBtn && modal) {
      closeBtn.addEventListener('click', function() {
        modal.classList.remove('show');

        // Limpiar cualquier dato temporal
        localStorage.removeItem('appointmentSuccess');
      });

      // Cerrar con ESC
      document.addEventListener('keydown', function(e) {
        if (e.key === 'Escape' && modal.classList.contains('show')) {
          modal.classList.remove('show');
          localStorage.removeItem('appointmentSuccess');
        }
      });

      // Cerrar al hacer click fuera del modal
      modal.addEventListener('click', function(e) {
        if (e.target === modal) {
          modal.classList.remove('show');
          localStorage.removeItem('appointmentSuccess');
        }
      });
    }

    // Verificar si hay una cita exitosa guardada en localStorage
    const appointmentSuccess = localStorage.getItem('appointmentSuccess');
    if (appointmentSuccess) {
      try {
        const data = JSON.parse(appointmentSuccess);
        window.showSuccessModal(data);
      } catch (error) {
        console.error('Error al mostrar modal de éxito:', error);
      }
    }
  });
</script>
