---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import ServiciosGrid from '../../components/ServiciosGrid.astro';
---

<Layout title="Integración CRM Inteligente - Aquí Creamos">
  <Header />

  <section class="pt-32 pb-20 bg-gradient-to-br from-[#00464e] via-[#006064] to-[#1a1a1a]">
    <div class="container mx-auto px-4 md:px-16">
      <div class="max-w-6xl mx-auto text-center">
        <div class="inline-block mb-4 px-4 py-2 bg-[#82e256]/20 border border-[#82e256] rounded-full">
          <span class="text-[#82e256] font-bold text-sm">🔄 INTEGRACIÓN CRM</span>
        </div>

        <h1 class="text-4xl md:text-6xl font-black text-white mb-6 leading-tight">
          Sincronización <span class="text-transparent bg-clip-text bg-gradient-to-r from-[#82e256] to-[#04ADC0]">inteligente de CRM</span>
        </h1>

        <p class="text-xl text-gray-300 mb-8 max-w-3xl mx-auto">
          Mantén sincronizados tus CRMs y bases de datos en tiempo real. Reduce errores en <strong class="text-[#82e256]">95%</strong> con validación de IA.
        </p>

        <div class="grid grid-cols-3 gap-4 mb-8 max-w-2xl mx-auto">
          <div class="bg-white/10 backdrop-blur rounded-lg p-4 border border-white/20">
            <div class="text-3xl font-black text-[#82e256]">95%</div>
            <div class="text-xs text-gray-300 mt-1">Menos errores</div>
          </div>
          <div class="bg-white/10 backdrop-blur rounded-lg p-4 border border-white/20">
            <div class="text-3xl font-black text-[#82e256]">100%</div>
            <div class="text-xs text-gray-300 mt-1">Sincronizado</div>
          </div>
          <div class="bg-white/10 backdrop-blur rounded-lg p-4 border border-white/20">
            <div class="text-3xl font-black text-[#82e256]">3x</div>
            <div class="text-xs text-gray-300 mt-1">Más leads</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Sticky Stack Section -->
  <div class="stack-container">
    <section class="sticky-section">
      <div class="content-grid">
        <div class="text-side">
          <span class="label">Sincronización</span>
          <h2>Multi-<br>Plataforma.</h2>
          <p class="desc">Mantén sincronizados HubSpot, Zoho CRM, PostgreSQL en tiempo real con validación de IA.</p>
        </div>
        <div class="image-side">
          <img src="https://images.unsplash.com/photo-1558655146-d09347e92766?q=80&w=1200" alt="Multi-Platform Sync">
        </div>
      </div>
    </section>

    <section class="sticky-section">
      <div class="content-grid reversed">
        <div class="text-side">
          <span class="label">Enriquecimiento</span>
          <h2>Datos<br>Completos.</h2>
          <p class="desc">Enriquece automáticamente contactos con información externa y valida campos con inteligencia artificial.</p>
        </div>
        <div class="image-side">
          <img src="https://images.unsplash.com/photo-1498050108023-c5249f4df085?q=80&w=1200" alt="Data Enrichment">
        </div>
      </div>
    </section>

    <section class="sticky-section">
      <div class="content-grid">
        <div class="text-side">
          <span class="label">Duplicados</span>
          <h2>Detección<br>Inteligente.</h2>
          <p class="desc">IA identifica registros duplicados, propone unificaciones y mantiene limpia tu base de datos.</p>
        </div>
        <div class="image-side">
          <img src="https://images.unsplash.com/photo-1451187530220-4e2bd78f7ad6?q=80&w=1200" alt="Duplicate Detection">
        </div>
      </div>
    </section>

    <section class="sticky-section">
      <div class="content-grid reversed">
        <div class="text-side">
          <span class="label">Validación</span>
          <h2>IA en<br>Tiempo Real.</h2>
          <p class="desc">Valida emails, teléfonos, direcciones y datos empresariales automáticamente con machine learning.</p>
        </div>
        <div class="image-side">
          <img src="https://images.unsplash.com/photo-1558655146-9f40138edfeb?q=80&w=1200" alt="AI Validation">
        </div>
      </div>
    </section>

    <section class="sticky-section">
      <div class="content-grid">
        <div class="text-side">
          <span class="label">Unificación</span>
          <h2>Vista Única<br>del Cliente.</h2>
          <p class="desc">Consolida datos de múltiples fuentes en un perfil completo y actualizado de cada cliente.</p>
        </div>
        <div class="image-side">
          <img src="https://images.unsplash.com/photo-1460925895917-afdab827c52f?q=80&w=1200" alt="Customer Unification">
        </div>
      </div>
    </section>

    <section class="sticky-section">
      <div class="content-grid reversed">
        <div class="text-side">
          <span class="label">Reportes</span>
          <h2>Insights<br>Automáticos.</h2>
          <p class="desc">Dashboards en tiempo real con métricas clave y análisis predictivo generado por IA.</p>
        </div>
        <div class="image-side">
          <img src="https://images.unsplash.com/photo-1551288049-bebda4e38f71?q=80&w=1200" alt="Automated Reports">
        </div>
      </div>
    </section>
  </div>

  <!-- Solutions Section -->
  <section class="py-16 bg-[#1a1a1a]">
    <div class="container mx-auto px-4 md:px-16">
      <div class="max-w-6xl mx-auto">
        <h2 class="text-3xl md:text-4xl font-black text-white mb-12 text-center">
          Automatizaciones de Integración CRM
        </h2>

        <div class="grid md:grid-cols-3 gap-6">
          <div class="bg-white/5 backdrop-blur border border-white/10 rounded-xl p-6 hover:border-[#82e256] transition-all">
🔄</div>
            <h3 class="text-xl font-bold text-white mb-3">Sincronización Multi-plataforma</h3>
            <p class="text-gray-300 mb-4">Mantén datos sincronizados entre HubSpot, Zoho CRM, PostgreSQL en tiempo real con validación de IA.</p>
            <p class="text-[#82e256] font-semibold text-sm">95% reducción en errores (Validación automática de datos con IA)</p>
          </div>

          <div class="bg-white/5 backdrop-blur border border-white/10 rounded-xl p-6 hover:border-[#82e256] transition-all">
🤝</div>
            <h3 class="text-xl font-bold text-white mb-3">Enriquecimiento de Datos</h3>
            <p class="text-gray-300 mb-4">Enriquece automáticamente datos de contactos con información externa y valida con IA.</p>
            <p class="text-[#82e256] font-semibold text-sm">3x más información por lead (Enriquecimiento automático de datos externos)</p>
          </div>

          <div class="bg-white/5 backdrop-blur border border-white/10 rounded-xl p-6 hover:border-[#82e256] transition-all">
📊</div>
            <h3 class="text-xl font-bold text-white mb-3">CRM Inteligente</h3>
            <p class="text-gray-300 mb-4">Plataforma CRM con agentes IA, clasificación automática de leads y reporting en tiempo real.</p>
            <p class="text-[#82e256] font-semibold text-sm">Clasificación automática de leads</p>
          </div>
        </div>

        <div class="text-center mt-12">
          <a href="/aquicreamos" class="text-[#82e256] hover:text-[#6bc73d] transition-colors font-semibold">
            Ver todas las automatizaciones de CRM →
          </a>
        </div>
      </div>
    </div>
  </section>

  <section class="py-20 bg-gradient-to-br from-[#00464e] to-[#006064]">
    <div class="container mx-auto px-4 md:px-16">
      <div class="max-w-4xl mx-auto text-center">
        <h2 class="text-3xl md:text-5xl font-black text-white mb-6">
          Integra y potencia tu CRM con IA
        </h2>
        <a href="/#contenido10" class="inline-block px-10 py-5 bg-gradient-to-r from-[#82e256] to-[#6bc73d] text-black rounded-lg font-bold text-lg hover:shadow-[0_0_40px_rgba(130,226,86,0.6)] transition-all duration-300 hover:scale-105">
          📅 Agendar Asesoría Gratuita
        </a>
      </div>
    </div>
  </section>

  <!-- Servicios Profesionales -->
  <ServiciosGrid />
</Layout>

<style>
  .stack-container { display: flex; flex-direction: column; }
  .sticky-section { position: sticky; top: 0; height: 100vh; display: flex; align-items: center; justify-content: center; overflow: hidden; background-color: #0e0e0e; transition: transform 0.1s linear, filter 0.1s linear; will-change: transform, filter; }
  .sticky-section:nth-child(1) { background-color: #0e0e0e; }
  .sticky-section:nth-child(2) { background-color: #161616; }
  .sticky-section:nth-child(3) { background-color: #1d1d1d; }
  .sticky-section:nth-child(4) { background-color: #0e0e0e; }
  .sticky-section:nth-child(5) { background-color: #161616; }
  .sticky-section:nth-child(6) { background-color: #1d1d1d; }
  .content-grid { display: grid; grid-template-columns: 1fr 1fr; width: 90%; max-width: 1300px; gap: 60px; align-items: center; }
  .text-side { display: flex; flex-direction: column; padding: 20px; }
  .label { font-weight: 600; text-transform: uppercase; letter-spacing: 2px; color: #82e256; margin-bottom: 10px; }
  .sticky-section h2 { font-size: clamp(2.5rem, 6vw, 5.5rem); line-height: 0.95; margin: 0; font-weight: 900; text-transform: uppercase; color: #fff; }
  p.desc { font-size: 1.2rem; margin-top: 30px; opacity: 0.7; max-width: 450px; line-height: 1.5; color: #fff; }
  .image-side { width: 100%; height: 65vh; border-radius: 15px; overflow: hidden; position: relative; }
  .image-side img { width: 100%; height: 100%; object-fit: cover; display: block; }
  .reversed .text-side { order: 2; }
  .reversed .image-side { order: 1; }
  @media (max-width: 900px) {
    .content-grid { grid-template-columns: 1fr; text-align: center; }
    .image-side { height: 35vh; order: -1 !important; }
    .text-side { align-items: center; padding: 0; }
    p.desc { margin-top: 15px; }
  }
</style>

<script>
  const sections = document.querySelectorAll('.sticky-section');
  window.addEventListener('scroll', () => {
    const viewportHeight = window.innerHeight;
    sections.forEach((section, index) => {
      const rect = section.getBoundingClientRect();
      if (rect.top <= 0) {
        const nextSection = sections[index + 1];
        if (nextSection) {
          const nextRect = nextSection.getBoundingClientRect();
          let progress = 1 - (nextRect.top / viewportHeight);
          progress = Math.max(0, Math.min(1, progress));
          const scale = 1 - (progress * 0.1);
          const brightness = 1 - (progress * 0.6);
          (section as HTMLElement).style.transform = `scale(${scale})`;
          (section as HTMLElement).style.filter = `brightness(${brightness})`;
        }
      } else {
        (section as HTMLElement).style.transform = `scale(1)`;
        (section as HTMLElement).style.filter = `brightness(1)`;
      }
    });
  });
</script>
